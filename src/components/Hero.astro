<main>
	<section class="hero" id="top" aria-labelledby="hero-title">
		<img
			class="hero-image"
			src="/images/hero.jpg"
			alt="The members of 4 Oceans under stage lights"
			fetchpriority="high"
		/>
		<div class="hero-shade"></div>
		<header class="site-header">
			<a class="brand" href="#top" aria-label="4 Oceans home">
				<img src="/images/logo.png" alt="4 Oceans" />
			</a>
			<nav aria-label="Main navigation">
				<a href="#about">EPK</a>
				<a href="#music">Music</a>
				<a href="#live">Live</a>
				<a href="#assets">Assets</a>
			</nav>
			<a class="booking-link" href="#booking">Booking</a>
		</header>

		<div class="hero-content">
			<p>Electronic press kit</p>
			<h1 id="hero-title" class="visually-hidden">4 Oceans</h1>
			<span>Berlin, Germany / Modern metalcore</span>
		</div>
	</section>

	<section class="press" id="about" aria-labelledby="about-title">
		<div class="section-inner press-layout">
			<div class="section-title">
				<p>About</p>
				<h2 id="about-title">4 Oceans</h2>
			</div>
			<div>
				<p class="press-text">
					4 Oceans is a Berlin-based modern metalcore band, formed in 2020 as a
					fully DIY project. The self-produced debut EP <em>First Wave</em> was
					followed by singles featuring Dominik of Cypecore and Egor Erushin of
					Abyss Watching Me. After touring Germany and Italy and passing 160,000
					Spotify streams, the band returns in 2026 with a renewed line-up and a
					new musical direction.
				</p>
				<dl class="facts">
					<div>
						<dt>Genre</dt>
						<dd>Modern metalcore</dd>
					</div>
					<div>
						<dt>Based</dt>
						<dd>Berlin, Germany</dd>
					</div>
					<div>
						<dt>Live</dt>
						<dd>Germany and Italy</dd>
					</div>
					<div>
						<dt>Streaming</dt>
						<dd>160k+ Spotify streams</dd>
					</div>
				</dl>
			</div>
		</div>
	</section>

	<section class="music" id="music" aria-labelledby="music-title">
		<div class="section-inner">
			<div class="section-heading-row">
				<div class="section-title">
					<p>Music</p>
					<h2 id="music-title">Listen</h2>
				</div>
				<div class="streaming-links">
					<a
						href="https://open.spotify.com/artist/4P8fDItPdqBc4sn5u5rUNX?si=krCyWmjbTVmjosQIfm8f8Q"
						target="_blank"
						rel="noopener noreferrer"
						>Spotify</a
					>
					<a
						href="https://music.apple.com/us/artist/4-oceans/1535860167"
						target="_blank"
						rel="noopener noreferrer"
						>Apple Music</a
					>
				</div>
			</div>
			<iframe
				title="4 Oceans on Spotify"
				src="https://open.spotify.com/embed/artist/4P8fDItPdqBc4sn5u5rUNX?utm_source=generator"
				width="100%"
				height="352"
				frameborder="0"
				allowfullscreen
				allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"
				loading="lazy"
			></iframe>
		</div>
	</section>

	<section class="live" id="live" aria-labelledby="live-title">
		<div class="live-image-wrap">
			<img src="/images/live-stage.jpg" alt="4 Oceans performing live on stage" loading="lazy" />
		</div>
		<div class="live-content">
			<p id="live-title">Live videos</p>
			<a
				class="action-link"
				href="https://www.youtube.com/c/4Oceansband"
				target="_blank"
				rel="noopener noreferrer"
				>Open YouTube channel</a
			>
		</div>
	</section>

	<section class="assets" id="assets" aria-labelledby="assets-title">
		<div class="section-inner">
			<div class="section-heading-row assets-heading">
				<div class="section-title">
					<p>Press materials</p>
					<h2 id="assets-title">Assets</h2>
				</div>
				<a class="download-link" href="/images/logo.png" download>Download logo (PNG)</a>
			</div>
			<div class="assets-grid">
				<figure>
					<img src="/images/press-band.jpg" alt="The members of 4 Oceans under stage lights" loading="lazy" />
					<figcaption><a href="/images/press-band.jpg" download>Press photo 01</a></figcaption>
				</figure>
				<figure>
					<img src="/images/press-live.jpg" alt="4 Oceans performing live under red lights" loading="lazy" />
					<figcaption><a href="/images/press-live.jpg" download>Press photo 02</a></figcaption>
				</figure>
			</div>
		</div>
	</section>

	<footer id="booking">
		<div class="section-inner footer-inner">
			<div class="contact-primary">
				<p>Booking and press</p>
				<a class="email" href="mailto:4oceans.berlin@gmail.com">4oceans.berlin@gmail.com</a>
			</div>
			<div class="social-links" aria-label="4 Oceans social links">
				<a href="/epk/4Oceans_TechRider_v1.4_Deutsch.pdf" download>Tech rider (DE)</a>
				<a href="https://www.instagram.com/4oceans_band/" target="_blank" rel="noopener noreferrer">Instagram</a>
				<a href="https://www.facebook.com/4Oceansband" target="_blank" rel="noopener noreferrer">Facebook</a>
				<a href="https://vk.com/4oceansband" target="_blank" rel="noopener noreferrer">VK</a>
				<a href="https://music.apple.com/us/artist/4-oceans/1535860167" target="_blank" rel="noopener noreferrer">Apple Music</a>
			</div>
		</div>
	</footer>
</main>

<style>
	:global(:root) {
		--ink: #0a0b0b;
		--paper: #efefeb;
		--muted: #8f928e;
		--line-dark: rgb(10 11 11 / 18%);
		--line-light: rgb(239 239 235 / 20%);
	}

	main {
		background: var(--ink);
		color: var(--paper);
		overflow: hidden;
	}

	a {
		color: inherit;
	}

	.visually-hidden {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}

	.hero {
		position: relative;
		z-index: 0;
		display: flex;
		flex-direction: column;
		min-height: 620px;
		height: min(82svh, 820px);
	}

	.hero-image,
	.hero-shade {
		position: absolute;
		inset: 0;
		z-index: -1;
		width: 100%;
		height: 100%;
	}

	.hero-image {
		object-fit: cover;
		object-position: 55% center;
	}

	.hero-shade {
		background: rgb(4 5 5 / 68%);
	}

	.section-inner,
	.site-header,
	.hero-content {
		width: min(100% - 64px, 1280px);
		margin-inline: auto;
	}

	.site-header {
		display: grid;
		grid-template-columns: 1fr auto 1fr;
		align-items: center;
		gap: 30px;
		padding: 25px 0;
	}

	.brand {
		width: 180px;
		display: block;
	}

	.brand img {
		width: 100%;
		height: auto;
		display: block;
	}

	nav,
	.social-links,
	.streaming-links {
		display: flex;
		align-items: center;
		gap: 22px;
	}

	nav a,
	.booking-link,
	.social-links a,
	.streaming-links a,
	.download-link,
	.assets-grid a {
		font-size: 11px;
		font-weight: 700;
		letter-spacing: 0.12em;
		text-decoration: none;
		text-transform: uppercase;
	}

	nav a:hover,
	.booking-link:hover,
	.social-links a:hover,
	.streaming-links a:hover,
	.download-link:hover,
	.assets-grid a:hover {
		color: var(--muted);
	}

	.booking-link {
		justify-self: end;
		border-bottom: 1px solid currentColor;
		padding-bottom: 4px;
	}

	.hero-content {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 10px;
		margin-block: auto;
		padding: 70px 0;
		font-size: 11px;
		font-weight: 700;
		letter-spacing: 0.14em;
		text-transform: uppercase;
	}

	.hero-content p,
	.hero-content span {
		margin: 0;
	}

	.hero-content p {
		color: var(--muted);
	}

	.press,
	.music,
	.assets {
		padding: clamp(64px, 8vw, 120px) 0;
	}

	.press {
		background: var(--paper);
		color: var(--ink);
	}

	.press-layout {
		display: grid;
		grid-template-columns: 1fr;
		gap: 38px;
	}

	.press-layout > .section-title {
		display: flex;
		align-items: baseline;
		gap: 22px;
		border-bottom: 1px solid var(--line-dark);
		padding-bottom: 22px;
	}

	.press-layout > .section-title h2 {
		margin: 0;
	}

	.section-title p,
	.footer-inner > p {
		margin: 0;
		font-size: 11px;
		font-weight: 700;
		letter-spacing: 0.14em;
		text-transform: uppercase;
	}

	h2 {
		margin: 14px 0 0;
		font-family: "Arial Black", Arial, sans-serif;
		font-size: clamp(32px, 4vw, 60px);
		font-weight: 900;
		letter-spacing: 0;
		line-height: 0.95;
		text-transform: uppercase;
	}

	.press-text {
		max-width: 980px;
		margin: 0;
		font-size: clamp(20px, 2vw, 29px);
		line-height: 1.34;
	}

	.facts {
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		margin: 38px 0 0;
		border-top: 1px solid var(--line-dark);
		border-bottom: 1px solid var(--line-dark);
	}

	.facts div {
		min-width: 0;
		padding: 16px;
	}

	.facts div:first-child {
		padding-left: 0;
	}

	.facts div + div {
		border-left: 1px solid var(--line-dark);
	}

	dt {
		margin-bottom: 7px;
		font-size: 10px;
		font-weight: 700;
		letter-spacing: 0.1em;
		text-transform: uppercase;
	}

	dd {
		margin: 0;
		font-size: 14px;
		line-height: 1.35;
	}

	.music {
		background: #141515;
	}

	.section-heading-row {
		display: flex;
		align-items: end;
		justify-content: space-between;
		gap: 32px;
		margin-bottom: 38px;
		border-bottom: 1px solid var(--line-light);
		padding-bottom: 24px;
	}

	.streaming-links {
		padding-bottom: 4px;
	}

	iframe {
		border: 0;
		border-radius: 4px;
	}

	.live {
		position: relative;
		height: clamp(420px, 56vw, 680px);
		background: var(--ink);
		color: var(--paper);
		isolation: isolate;
	}

	.live::after {
		position: absolute;
		z-index: -1;
		inset: 0;
		background: rgb(0 0 0 / 24%);
		content: "";
	}

	.live-image-wrap {
		position: absolute;
		z-index: -2;
		inset: 0;
		height: 100%;
	}

	.live-image-wrap img {
		width: 100%;
		height: 100%;
		display: block;
		object-fit: cover;
	}

	.live-content {
		position: absolute;
		z-index: 1;
		bottom: 30px;
		left: max(32px, calc((100vw - 1280px) / 2));
		right: max(32px, calc((100vw - 1280px) / 2));
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 30px;
	}

	.live-content > p {
		margin: 0;
		font-size: 11px;
		font-weight: 700;
		letter-spacing: 0.14em;
		text-transform: uppercase;
	}

	.action-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 46px;
		border: 1px solid var(--paper);
		border-radius: 3px;
		padding: 0 18px;
		font-size: 11px;
		font-weight: 700;
		letter-spacing: 0.1em;
		text-decoration: none;
		text-transform: uppercase;
	}

	.action-link:hover {
		background: var(--paper);
		color: var(--ink);
	}

	.assets {
		background: var(--ink);
		padding-bottom: 64px;
	}

	.assets-heading {
		margin-bottom: 34px;
	}

	.download-link {
		border-bottom: 1px solid currentColor;
		padding-bottom: 5px;
	}

	.assets-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 20px;
	}

	figure {
		margin: 0;
	}

	.assets-grid img {
		width: 100%;
		aspect-ratio: 3 / 2;
		height: auto;
		display: block;
		object-fit: cover;
	}

	.assets-grid figure:first-child img {
		object-position: center 34%;
	}

	.assets-grid figcaption {
		border-bottom: 1px solid var(--line-light);
		padding: 14px 0;
	}

	footer {
		border-top: 1px solid var(--line-light);
		background: #050606;
	}

	.footer-inner {
		display: flex;
		align-items: center;
		justify-content: space-between;
		flex-wrap: wrap;
		gap: 30px;
		padding: 38px 0;
	}

	.contact-primary {
		display: flex;
		align-items: center;
		gap: 24px;
		min-width: 0;
	}

	.email {
		border-bottom: 1px solid currentColor;
		padding-bottom: 3px;
		font-size: clamp(16px, 1.1vw, 18px);
		font-weight: 500;
		letter-spacing: 0.02em;
		line-height: 1.2;
		text-decoration: none;
		white-space: nowrap;
	}

	.social-links {
		margin-left: auto;
		justify-content: flex-end;
		flex-wrap: wrap;
	}

	@media (max-width: 900px) {
		.press-layout {
			gap: 38px;
		}

		.footer-inner {
			align-items: flex-start;
		}

		.contact-primary {
			gap: 24px;
		}
	}

	@media (max-width: 640px) {
		.hero {
			min-height: 580px;
			height: 80svh;
		}

		.section-inner,
		.site-header,
		.hero-content {
			width: min(100% - 40px, 1280px);
		}

		.site-header {
			grid-template-columns: 1fr auto;
			padding: 20px 0;
		}

		.brand {
			width: 148px;
		}

		nav {
			display: none;
		}

		.press,
		.music,
		.assets {
			padding: 60px 0;
		}

		.facts {
			grid-template-columns: repeat(2, minmax(0, 1fr));
			margin-top: 38px;
		}

		.facts div {
			padding: 14px;
		}

		.facts div:first-child,
		.facts div:nth-child(3) {
			padding-left: 0;
		}

		.facts div:nth-child(3) {
			border-left: 0;
		}

		.section-heading-row {
			align-items: flex-start;
			flex-direction: column;
			gap: 26px;
			margin-bottom: 28px;
		}

		.streaming-links {
			padding-bottom: 0;
		}

		.live-content {
			left: 20px;
			right: 20px;
			bottom: 20px;
		}

		.assets-grid {
			grid-template-columns: 1fr;
			gap: 28px;
		}

		.footer-inner {
			gap: 24px;
			padding: 34px 0;
		}

		.contact-primary {
			align-items: flex-start;
			flex-direction: column;
			gap: 14px;
		}

		.social-links {
			margin-left: 0;
			justify-content: flex-start;
			flex-wrap: wrap;
			gap: 16px;
		}
	}
</style>
